apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zm-schiss-training-pipeline-chart.fullname" . }}-test-connection"
  labels:
    {{- include "zm-schiss-training-pipeline-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zm-schiss-training-pipeline-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
